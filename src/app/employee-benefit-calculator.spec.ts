import { EmployeeBenefitCalculator } from './employee-benefit-calculator';

describe('BenefitCalculator', () => {
  it('should create an instance', () => {
    expect(new EmployeeBenefitCalculator()).toBeTruthy();
  });
});
